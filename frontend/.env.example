# Frontend environment flags (Vite). Copy to `.env.local` to override.
#
# Force-enable experimental/provisional surfaces (Protocol Lab, MIDI slot editor,
# pedal editor) in a production build. On automatically during `vite dev`.
# @see docs/specs/210-frontend-ipc-contracts/spec.md [FR-26]
VITE_EXPERIMENTAL=false
